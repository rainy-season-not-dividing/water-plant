CONTEXT_PACKAGE_POLICY = {
    "default": "Keep only task-scoped telemetry, history, and evidence for each Agent.",
    "legacy_compatibility": "The compatibility path preserves the original telemetry payload.",
}
